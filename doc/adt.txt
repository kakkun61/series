-- Model

data Account =
    Account
        { id :: Word
        , identifier :: Text
        , name :: Text
        , serieses :: [Series]
        }

data Series
    = Series
        { id :: Word
        , title :: Text
        , articles :: [Article]
        }
    | AnalyzingSeries
        { id :: Word }

data Article =
    Article
        { id :: Word
        , title :: Text
        , read :: Bool
        , number :: Word
        }

-- DB Model

data Account =
    Account
        { id :: Word
        , identifier :: Text
        , name :: Text
        }

data Series
    = Series
        { id :: Word
        , title :: Text
        }
    | AnalyzingSeries
        { id :: Word }

data Following =
    Following
        { id :: Word
        , accountId :: Word
        }
